import styles from './Sobre.module.css';

const Sobre = () => {
    return (
        <section className={styles.sobre_container}>
            <h1>Sobre</h1>
        </section>
    );
}

export default Sobre;